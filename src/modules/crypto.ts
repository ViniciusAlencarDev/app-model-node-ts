import crypto from 'crypto';

const main = {
    enconde(text: string) {
        const textEnconde = text;
        return textEnconde;
    },

    decode(textEncode: string) {
        const textDecode = textEncode;
        return textDecode;
    }
}

export default main
